<div class="wizard">
    <div class="container">
        <span class="active">@("Basic info".Localize())<b></b></span>
        <span class="@(ViewBag.Step > 1 ? "active" : "")">@("Conditions".Localize())<b></b></span>
        <span class="@(ViewBag.Step > 2 ? "active" : "")">@("Policy".Localize())<b></b></span>
        <span class="@(ViewBag.Step > 3 ? "active" : "")">@("Complete".Localize())<b></b></span>
    </div>
</div>