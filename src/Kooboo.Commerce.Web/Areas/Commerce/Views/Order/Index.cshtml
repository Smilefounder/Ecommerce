@using Kooboo.Commerce.Orders
@{
    ViewBag.Title = "Orders".Localize();
    ViewBag.EnableSearch = true;
}

@section Panel {
    @(Html.HeaderPanel().Buttons(buttons =>
    {
        buttons.AddTopbarCommands(TopbarCommands.GetCommands(ViewContext.Controller.ControllerContext), typeof(Order));
    }))
}

@Html.Partial("_TabQuery")
